machine:
  node:
    version: 7.3.0

dependencies:
  override:
    - ./scripts/install-shared-libararies.bash
    - make init
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - make ci

